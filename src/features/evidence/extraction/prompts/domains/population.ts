import type { PromptDomainDefinition } from "../../contracts/prompt";

export const POPULATION_DOMAIN: PromptDomainDefinition = {
  id: "population",
  name: "Population",
  description:
    "Prompts describing study populations — inclusion, exclusion, age, ethnicity, geography, comorbidities.",
  prompts: [],
};
