import type { PromptDomainDefinition } from "../../contracts/prompt";

export const WOMEN_DOMAIN: PromptDomainDefinition = {
  id: "women",
  name: "Women",
  description:
    "Extraction prompts framing evidence around women as a first-class population — sex-specific results, subgroup reporting, exclusion patterns.",
  prompts: [],
};
